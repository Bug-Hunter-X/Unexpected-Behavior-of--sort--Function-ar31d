```haskell
import Data.List (sort)

main :: IO ()
main = do
  let xs = [1, 2, 3, 4, 5]
  let ys = sort xs
  print ys -- Correctly prints the sorted list
  print xs -- Prints the original list, unchanged
```